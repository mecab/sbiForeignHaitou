import assert = require('assert');

// eslint-disable-next-line @typescript-eslint/no-empty-function
describe('currently no test is available.', function() {
    it ("let's assert", async function() {
        assert(1 === 1);
    });
});
